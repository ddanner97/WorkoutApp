import React from 'react'

function Parameters( parameters ) {
    console.log(parameters.parameters)
    // const reps = parameters.parameters.reps

    return (
        <div>
            
        </div>
    )
}

export default Parameters